import {Metadata} from "next";

export const metadata: Metadata = {
  title:'customer'
}

const Page = () => {
    return (
        <p>
           customers Page
        </p>
    );
};

export default Page;